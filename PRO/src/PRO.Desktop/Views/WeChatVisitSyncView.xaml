<UserControl x:Class="PRO.Desktop.Views.WeChatVisitSyncView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" Background="{StaticResource ContentBackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="企业微信拜访记录同步" Style="{StaticResource PageTitleStyle}"/>
            <TextBlock Text="同步企业微信智能表格中的拜访记录，关联客户后自动从表格删除。" FontSize="11" Foreground="{StaticResource TertiaryLabelBrush}" Margin="0,2,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12">
            <Button Content="🔄 从企业微信同步" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SyncFromWeChatCommand}" Padding="16,7"/>
            <TextBlock Text="{Binding SyncStatus}" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="{StaticResource AccentBrush}" FontSize="12"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Records}" SelectedItem="{Binding SelectedRecord}"
                  Style="{StaticResource DataGridStyle}" AutoGenerateColumns="False" RowHeight="38" Margin="0,12,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="客户名称" Binding="{Binding CustomerName}" Width="*"/>
                <DataGridTextColumn Header="客户担当" Binding="{Binding CustomerManagerName}" Width="120"/>
                <DataGridTextColumn Header="日期" Binding="{Binding VisitDate, StringFormat={}{0:MM-dd}}" Width="60"/>
                <DataGridTextColumn Header="目的" Binding="{Binding Purpose}" Width="100"/>
                <DataGridTextColumn Header="同步时间" Binding="{Binding SyncedAt, StringFormat={}{0:MM-dd HH:mm}}" Width="120"/>
                <DataGridTemplateColumn Header="操作" Width="240">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="关联客户" Style="{StaticResource PlainButtonStyle}" FontSize="11"
                                        Foreground="{StaticResource AccentBrush}" Padding="4,2"
                                        Command="{Binding DataContext.LinkToCustomerCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="快速创建" Style="{StaticResource PlainButtonStyle}" FontSize="11"
                                        Foreground="#27AE60" Padding="4,2" Margin="4,0,0,0"
                                        Command="{Binding DataContext.QuickCreateCustomerCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="详情" Style="{StaticResource PlainButtonStyle}" FontSize="11"
                                        Foreground="{StaticResource AccentBrush}" Padding="4,2" Margin="4,0,0,0"
                                        Command="{Binding DataContext.ViewDetailCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
