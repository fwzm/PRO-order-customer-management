<Window x:Class="PRO.Desktop.Views.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="修改密码"
        Height="420" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}"
        TextElement.Foreground="{StaticResource TextPrimaryBrush}">

    <Border Margin="24" Background="{StaticResource SurfaceBrush}" CornerRadius="12"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <StackPanel Margin="28">
            <!-- 标题 -->
            <TextBlock Text="安全提示" FontSize="20" FontWeight="SemiBold"
                       Foreground="{StaticResource LabelBrush}" Margin="0,0,0,8"/>
            <TextBlock Text="您当前使用的是默认密码，为保障账户安全，请立即修改密码。"
                       Foreground="{StaticResource SecondaryLabelBrush}" FontSize="13"
                       TextWrapping="Wrap" Margin="0,0,0,20"/>

            <!-- 新密码 -->
            <TextBlock Text="新密码" FontSize="13" FontWeight="Medium"
                       Foreground="{StaticResource TertiaryLabelBrush}" Margin="0,0,0,6"/>
            <PasswordBox x:Name="txtNewPassword" Padding="12,9" FontSize="13" Margin="0,0,0,12">
                <PasswordBox.Style>
                    <Style TargetType="PasswordBox">
                        <Setter Property="BorderBrush" Value="{StaticResource FillBrush}"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Background" Value="{StaticResource ContentBackgroundBrush}"/>
                        <Setter Property="Foreground" Value="{StaticResource LabelBrush}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="PasswordBox">
                                    <Border x:Name="border" Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="9">
                                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </PasswordBox.Style>
            </PasswordBox>

            <!-- 确认密码 -->
            <TextBlock Text="确认密码" FontSize="13" FontWeight="Medium"
                       Foreground="{StaticResource TertiaryLabelBrush}" Margin="0,0,0,6"/>
            <PasswordBox x:Name="txtConfirmPassword" Padding="12,9" FontSize="13" Margin="0,0,0,12">
                <PasswordBox.Style>
                    <Style TargetType="PasswordBox">
                        <Setter Property="BorderBrush" Value="{StaticResource FillBrush}"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Background" Value="{StaticResource ContentBackgroundBrush}"/>
                        <Setter Property="Foreground" Value="{StaticResource LabelBrush}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="PasswordBox">
                                    <Border x:Name="border" Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="9">
                                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </PasswordBox.Style>
            </PasswordBox>

            <!-- 密码要求提示 -->
            <TextBlock FontSize="11" Foreground="{StaticResource TertiaryLabelBrush}" Margin="0,0,0,16">
                <Run Text="密码要求："/>
                <Run Text="至少8位，包含大小写字母和数字"/>
            </TextBlock>

            <!-- 错误信息 -->
            <TextBlock x:Name="txtError" Foreground="{StaticResource ErrorBrush}" FontSize="12"
                       HorizontalAlignment="Center" Margin="0,0,0,12" Visibility="Collapsed"/>

            <!-- 按钮 -->
            <Button x:Name="btnConfirm" Content="确认修改" 
                    Style="{StaticResource PrimaryButtonStyle}"
                    Height="44" FontSize="14" FontWeight="Medium"
                    Click="ConfirmButton_Click"/>
        </StackPanel>
    </Border>
</Window>
