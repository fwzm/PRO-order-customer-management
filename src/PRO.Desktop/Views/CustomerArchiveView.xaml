<UserControl x:Class="PRO.Desktop.Views.CustomerArchiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             Background="{StaticResource WindowBackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 加载中 -->
        <Border Grid.Row="0" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                Background="{StaticResource AccentLightBrush}" Padding="12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="Loading" Width="16" Height="16" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center">
                    <materialDesign:PackIcon.RenderTransform>
                        <RotateTransform CenterX="8" CenterY="8"/>
                    </materialDesign:PackIcon.RenderTransform>
                </materialDesign:PackIcon>
                <TextBlock Text="加载客户档案中..." Margin="8,0,0,0" Foreground="{StaticResource AccentBrush}"/>
            </StackPanel>
        </Border>

        <!-- 主内容 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24">
            <StackPanel Visibility="{Binding Archive, Converter={StaticResource NullToVisibilityConverter}}">

                <!-- ========== 客户头部信息 ========== -->
                <Border Style="{StaticResource AppleCardStyle}" Margin="0,0,0,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Archive.Name}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource LabelBrush}"/>
                                <Border Background="{StaticResource AccentLightBrush}" CornerRadius="4" Padding="8,4" Margin="12,0">
                                    <TextBlock Text="{Binding Archive.CustomerTypeName}" FontSize="12" Foreground="{StaticResource AccentBrush}"/>
                                </Border>
                                <Border Visibility="{Binding Archive.IsWeChatBound, Converter={StaticResource BoolToVisibilityConverter}}"
                                        Background="#E8F5E9" CornerRadius="4" Padding="8,4" Margin="8,0">
                                    <TextBlock Text="企微已绑定" FontSize="12" Foreground="#2E7D32"/>
                                </Border>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Foreground="{StaticResource SecondaryLabelBrush}" FontSize="13">
                                    <Run Text="编号："/><Run Text="{Binding Archive.CustomerNo}"/>
                                </TextBlock>
                                <TextBlock Foreground="{StaticResource SecondaryLabelBrush}" FontSize="13" Margin="20,0,0,0">
                                    <Run Text="分公司："/><Run Text="{Binding Archive.BranchName}"/>
                                </TextBlock>
                                <TextBlock Foreground="{StaticResource SecondaryLabelBrush}" FontSize="13" Margin="20,0,0,0">
                                    <Run Text="创建："/><Run Text="{Binding Archive.CreatedAt, StringFormat=yyyy-MM-dd}"/>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>

                        <Button Grid.Column="1" Content="刷新" Style="{StaticResource SecondaryButtonStyle}"
                                Command="{Binding RefreshCommand}" Padding="12,6"/>
                    </Grid>
                </Border>

                <!-- ========== 风险提示（有风险时显示） ========== -->
                <ItemsControl ItemsSource="{Binding Archive.Risks}" Margin="0,0,0,16">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,6" Padding="12,10" CornerRadius="8">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#FFF3CD"/>
                                        <Setter Property="BorderBrush" Value="#FFEEBA"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Type}" Value="Error">
                                                <Setter Property="Background" Value="#F8D7DA"/>
                                                <Setter Property="BorderBrush" Value="#F5C6CB"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <TextBlock Text="{Binding Message}" FontWeight="SemiBold" FontSize="13"/>
                                    <TextBlock Text="{Binding Suggestion}" FontSize="12" Foreground="{StaticResource SecondaryLabelBrush}" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- ========== 核心指标 ========== -->
                <UniformGrid Rows="1" Margin="0,0,0,16">
                    <Border Style="{StaticResource AppleCardStyle}" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="累计订单" FontSize="12" Foreground="{StaticResource TertiaryLabelBrush}"/>
                            <TextBlock FontSize="28" FontWeight="Bold" Foreground="{StaticResource LabelBrush}" Margin="0,6,0,0">
                                <Run Text="{Binding Archive.TotalOrderCount}"/><Run Text=" 单" FontSize="14"/>
                            </TextBlock>
                            <TextBlock FontSize="13" Foreground="{StaticResource SecondaryLabelBrush}">
                                <Run Text="¥"/><Run Text="{Binding Archive.TotalOrderAmount, StringFormat=N0}"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource AppleCardStyle}" Margin="4,0,4,0">
                        <StackPanel>
                            <TextBlock Text="本月订单" FontSize="12" Foreground="{StaticResource TertiaryLabelBrush}"/>
                            <TextBlock FontSize="28" FontWeight="Bold" Foreground="{StaticResource LabelBrush}" Margin="0,6,0,0">
                                <Run Text="{Binding Archive.ThisMonthOrderCount}"/><Run Text=" 单" FontSize="14"/>
                            </TextBlock>
                            <TextBlock FontSize="13" Foreground="{StaticResource SecondaryLabelBrush}">
                                <Run Text="¥"/><Run Text="{Binding Archive.ThisMonthOrderAmount, StringFormat=N0}"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource AppleCardStyle}" Margin="4,0,4,0">
                        <StackPanel>
                            <TextBlock Text="应收账款" FontSize="12" Foreground="{StaticResource TertiaryLabelBrush}"/>
                            <TextBlock FontSize="28" FontWeight="Bold" Foreground="{StaticResource ErrorBrush}" Margin="0,6,0,0">
                                <Run Text="¥"/><Run Text="{Binding Archive.TotalReceivable, StringFormat=N0}"/>
                            </TextBlock>
                            <TextBlock FontSize="13" Foreground="{StaticResource ErrorBrush}">
                                <Run Text="{Binding Archive.OverdueOrderCount}"/><Run Text=" 笔超期"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource AppleCardStyle}" Margin="8,0,0,0">
                        <StackPanel>
                            <TextBlock Text="最近下单" FontSize="12" Foreground="{StaticResource TertiaryLabelBrush}"/>
                            <TextBlock FontSize="28" FontWeight="Bold" Foreground="{StaticResource LabelBrush}" Margin="0,6,0,0">
                                <Run Text="{Binding Archive.DaysSinceLastOrder}"/><Run Text=" 天前" FontSize="14"/>
                            </TextBlock>
                            <TextBlock Text="{Binding Archive.LastOrderDate, StringFormat=yyyy-MM-dd}" FontSize="13" Foreground="{StaticResource SecondaryLabelBrush}"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- ========== 基本信息 + 联系方式 ========== -->
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource AppleCardStyle}" Margin="0,0,4,0">
                        <StackPanel>
                            <TextBlock Text="基本信息" FontWeight="SemiBold" Margin="0,0,0,12"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="手机号" Foreground="{StaticResource TertiaryLabelBrush}" Margin="0,0,0,8"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Archive.Phone, FallbackValue=未填写}" Margin="0,0,0,8"/>

                                <TextBlock Grid.Row="1" Text="法人" Foreground="{StaticResource TertiaryLabelBrush}" Margin="0,0,0,8"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Archive.LegalPerson, FallbackValue=未填写}" Margin="0,0,0,8"/>

                                <TextBlock Grid.Row="2" Text="地址" Foreground="{StaticResource TertiaryLabelBrush}" Margin="0,0,0,8"/>
                                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Archive.FullAddress, FallbackValue=未填写}" TextWrapping="Wrap" Margin="0,0,0,8"/>

                                <TextBlock Grid.Row="3" Text="备注" Foreground="{StaticResource TertiaryLabelBrush}"/>
                                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Archive.Remark, FallbackValue=无}" TextWrapping="Wrap"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Style="{StaticResource AppleCardStyle}" Margin="4,0,0,0">
                        <StackPanel>
                            <TextBlock Text="关联信息" FontWeight="SemiBold" Margin="0,0,0,12"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="所属大客户" Foreground="{StaticResource TertiaryLabelBrush}" Margin="0,0,0,8"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Archive.ParentCustomerName, FallbackValue=无}" Margin="0,0,0,8"/>

                                <TextBlock Grid.Row="1" Text="子客户数" Foreground="{StaticResource TertiaryLabelBrush}" Margin="0,0,0,8"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Archive.SubCustomers.Count}" Margin="0,0,0,8"/>

                                <TextBlock Grid.Row="2" Text="企微状态" Foreground="{StaticResource TertiaryLabelBrush}"/>
                                <TextBlock Grid.Row="2" Grid.Column="1">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="未绑定"/>
                                            <Setter Property="Foreground" Value="{StaticResource TertiaryLabelBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Archive.IsWeChatBound}" Value="True">
                                                    <Setter Property="Text" Value="已绑定"/>
                                                    <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- ========== 客户时间线 ========== -->
                <Border Style="{StaticResource AppleCardStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <materialDesign:PackIcon Kind="ClockOutline" Width="20" Height="20"
                                                     Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="客户时间线" FontWeight="SemiBold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ItemsControl ItemsSource="{Binding TimelineEvents}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="36"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Width="28" Height="28" CornerRadius="14" Background="{Binding EventColor}"
                                                HorizontalAlignment="Center" VerticalAlignment="Top">
                                            <TextBlock Text="{Binding EventIcon}" FontSize="13"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="12">
                                            <StackPanel>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                                                    <TextBlock Grid.Column="1" Text="{Binding EventTime, StringFormat=yyyy-MM-dd HH:mm}"
                                                               FontSize="12" Foreground="{StaticResource TertiaryLabelBrush}"/>
                                                </Grid>
                                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap"
                                                           Foreground="{StaticResource SecondaryLabelBrush}" Margin="0,4,0,0"/>
                                                <TextBlock Text="{Binding OperatorName}" FontSize="12"
                                                           Foreground="{StaticResource TertiaryLabelBrush}" Margin="0,4,0,0"/>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Border Visibility="{Binding TimelineEvents.Count, Converter={StaticResource ZeroToVisibilityConverter}}"
                                Padding="12">
                            <TextBlock Text="暂无时间线记录" Foreground="{StaticResource TertiaryLabelBrush}" HorizontalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- ========== 最近订单 ========== -->
                <Border Style="{StaticResource AppleCardStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="最近订单" FontWeight="SemiBold" Margin="0,0,0,12"/>
                        <DataGrid ItemsSource="{Binding Archive.RecentOrders}" IsReadOnly="True" MaxHeight="250"
                                  AutoGenerateColumns="False" GridLinesVisibility="Horizontal">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="订单号" Binding="{Binding OrderNo}" Width="130"/>
                                <DataGridTextColumn Header="金额" Binding="{Binding TotalAmount, StringFormat=¥{0:N2}}" Width="100"/>
                                <DataGridTextColumn Header="状态" Binding="{Binding StatusName}" Width="80"/>
                                <DataGridTextColumn Header="时间" Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="140"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <!-- ========== 拜访记录 ========== -->
                <Border Style="{StaticResource AppleCardStyle}">
                    <StackPanel>
                        <TextBlock Text="最近拜访" FontWeight="SemiBold" Margin="0,0,0,12"/>
                        <ItemsControl ItemsSource="{Binding Archive.RecentVisits}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,8" Padding="12" Background="{StaticResource SurfaceBrush}" CornerRadius="8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding VisitDate, StringFormat=MM-dd}" FontWeight="SemiBold"/>
                                                <TextBlock Text="{Binding CreatedByName}" FontSize="11" Foreground="{StaticResource TertiaryLabelBrush}"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding Purpose, FallbackValue=无目的}" FontWeight="Medium"/>
                                                <TextBlock Text="{Binding Result, FallbackValue=无记录}" FontSize="12" Foreground="{StaticResource SecondaryLabelBrush}" Margin="0,2,0,0"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
