<UserControl x:Class="PRO.Desktop.Views.ProductListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="产品管理" Style="{StaticResource PageTitleStyle}" Margin="0"/>
                
                <Button Grid.Column="1" Content="新增产品" Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding NewProductCommand}"/>
            </Grid>
        </Border>

        <!-- 筛选栏 -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" Padding="24,12"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBoxStyle}"
                         Width="250" Margin="0,0,12,0"
                         materialDesign:HintAssist.Hint="搜索产品名称/SKU..."/>
                
                <ComboBox Width="120" Margin="0,0,12,0"
                          Style="{StaticResource AppleComboBoxStyle}"
                          materialDesign:HintAssist.Hint="产品状态"
                          SelectedValue="{Binding FilterStatus}" SelectedValuePath="Tag">
                    <ComboBoxItem Content="全部" IsSelected="True"/>
                    <ComboBoxItem Content="启用" Tag="Active"/>
                    <ComboBoxItem Content="停用" Tag="Inactive"/>
                </ComboBox>
                
                <ComboBox Width="120" Margin="0,0,12,0"
                          Style="{StaticResource AppleComboBoxStyle}"
                          materialDesign:HintAssist.Hint="产品分类"
                          ItemsSource="{Binding CategoriesWithAll}"
                          DisplayMemberPath="Name"
                          SelectedValue="{Binding FilterCategoryId}" SelectedValuePath="Id">
                </ComboBox>
                
                <Button Content="搜索" Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding SearchCommand}" Margin="0,0,8,0"/>
                <Button Content="刷新" Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding RefreshCommand}"/>
            </StackPanel>
        </Border>

        <!-- 数据表格 -->
        <Border Grid.Row="2" Margin="24,16" Style="{StaticResource CardStyle}" Padding="0">
            <Grid>
            <DataGrid ItemsSource="{Binding Products}"
                      SelectedItem="{Binding SelectedProduct}"
                      Style="{StaticResource DataGridStyle}"
                      IsReadOnly="True">
                <DataGrid.InputBindings>
                    <KeyBinding Gesture="Ctrl+N" Command="{Binding NewProductCommand}"/>
                </DataGrid.InputBindings>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="140" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="产品名称" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="规格" Binding="{Binding Specification}" Width="100"/>
                    <DataGridTextColumn Header="销售均价" Binding="{Binding AverageSalePrice, StringFormat=¥{0:N2}}" Width="90"/>
                    <DataGridTextColumn Header="参考价" Binding="{Binding ReferencePrice, StringFormat=¥{0:N2}}" Width="90"/>
                    <DataGridTextColumn Header="库存" Binding="{Binding Stock}" Width="70"/>
                    <DataGridTextColumn Header="单位" Binding="{Binding Unit}" Width="60"/>
                    <DataGridTextColumn Header="分类" Binding="{Binding CategoryName}" Width="80"/>
                    <DataGridTemplateColumn Header="状态" Width="70">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource StatusTagStyle}"
                                        Background="{Binding Status, Converter={StaticResource ProductStatusColorConverter}}">
                                    <TextBlock Text="{Binding Status, Converter={StaticResource ProductStatusTextConverter}}" 
                                               Foreground="White" FontSize="11"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="操作" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="编辑" Padding="8,4" FontSize="11"
                                        Command="{Binding DataContext.EditProductCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource SecondaryButtonStyle}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <Border Background="Transparent"
                    Visibility="{Binding Products.Count, Converter={StaticResource ZeroToVisibilityConverter}}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="暂无产品" FontSize="20" Foreground="{StaticResource QuaternaryLabelBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="点击右上角「新增产品」开始" FontSize="13" Foreground="{StaticResource QuaternaryLabelBrush}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
            </Grid>
        </Border>

        <!-- 分页栏 -->
        <Border Grid.Row="3" Background="{StaticResource SurfaceBrush}" Padding="24,12"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center">
                    <Run Text="共"/>
                    <Run Text="{Binding TotalCount}"/>
                    <Run Text="条记录"/>
                </TextBlock>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="首页" Command="{Binding FirstPageCommand}"
                            Style="{StaticResource PaginationButtonStyle}" Margin="2,0"/>
                    <Button Content="上一页" Command="{Binding PreviousPageCommand}"
                            Style="{StaticResource PaginationButtonStyle}" Margin="2,0"/>
                    <TextBlock VerticalAlignment="Center" Margin="12,0">
                        <Run Text="第"/>
                        <Run Text="{Binding PageIndex}"/>
                        <Run Text="/"/>
                        <Run Text="{Binding TotalPages}"/>
                        <Run Text="页"/>
                    </TextBlock>
                    <Button Content="下一页" Command="{Binding NextPageCommand}"
                            Style="{StaticResource PaginationButtonStyle}" Margin="2,0"/>
                    <Button Content="末页" Command="{Binding LastPageCommand}"
                            Style="{StaticResource PaginationButtonStyle}" Margin="2,0"/>
                    <ComboBox SelectedValue="{Binding PageSize}" SelectedValuePath="Tag" Margin="16,0,0,0" Width="80"
                              Style="{StaticResource AppleComboBoxStyle}">
                        <ComboBoxItem Content="50" Tag="50"/>
                        <ComboBoxItem Content="100" Tag="100" IsSelected="True"/>
                        <ComboBoxItem Content="200" Tag="200"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 加载中 -->
        <Border Grid.RowSpan="4" Background="#80FFFFFF"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200"/>
                <TextBlock Text="加载中..." Margin="0,8,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
