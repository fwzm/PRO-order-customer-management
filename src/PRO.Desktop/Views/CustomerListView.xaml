<UserControl x:Class="PRO.Desktop.Views.CustomerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="客户管理" Style="{StaticResource PageTitleStyle}" Margin="0"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="新增客户" Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding NewCustomerCommand}" Margin="0,0,8,0"/>
                    <Button Content="合并客户" Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding MergeCustomersCommand}" Margin="0,0,8,0"/>
                    <Button Content="导出Excel" Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding ExportToExcelCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 筛选栏 -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" Padding="24,12" 
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBoxStyle}"
                         Width="250" Margin="0,0,12,0"
                         materialDesign:HintAssist.Hint="搜索客户名称、手机号..."/>
                
                <CheckBox Content="仅显示大客户" IsChecked="{Binding ShowMajorOnly}"
                          Margin="0,0,12,0" VerticalAlignment="Center"/>
                
                <Button Content="搜索" Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding SearchCommand}" Margin="0,0,8,0"/>
                <Button Content="刷新" Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding RefreshCommand}"/>
            </StackPanel>
        </Border>

        <!-- 数据表格 -->
        <Border Grid.Row="2" Margin="24,16" Style="{StaticResource CardStyle}" Padding="0">
            <Grid>
            <DataGrid ItemsSource="{Binding Customers}"
                      SelectedItem="{Binding SelectedCustomer}"
                      Style="{StaticResource DataGridStyle}"
                      IsReadOnly="True" CanUserResizeColumns="True">
                <DataGrid.InputBindings>
                    <KeyBinding Gesture="Ctrl+N" Command="{Binding NewCustomerCommand}"/>
                </DataGrid.InputBindings>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="客户名称" Binding="{Binding Name}" Width="2*"/>
                    <DataGridTemplateColumn Header="类型" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="4" Padding="6,2"
                                        Background="{Binding CustomerType, Converter={StaticResource CustomerTypeColorConverter}}">
                                    <TextBlock Text="{Binding CustomerTypeName}" Foreground="White" FontSize="10" HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="手机号" Binding="{Binding Phone}" Width="100"/>
                    <DataGridTextColumn Header="分公司" Binding="{Binding BranchName}" Width="100"/>
                    <DataGridTextColumn Header="客户担当" Binding="{Binding CustomerManagerName}" Width="80"/>
                    <DataGridTextColumn Header="订单数" Binding="{Binding OrderCount}" Width="55"/>
                    <DataGridTextColumn Header="创建时间" Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd}" Width="85"/>
                    <DataGridTemplateColumn Header="操作" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="查看" Padding="8,4" FontSize="11"
                                            Command="{Binding DataContext.ViewCustomerDetailCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,4,0"/>
                                    <Button Content="删除" Padding="8,4" FontSize="11" Foreground="{StaticResource ErrorBrush}"
                                            Command="{Binding DataContext.DeleteCustomerCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource PlainButtonStyle}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <Border Background="Transparent"
                    Visibility="{Binding Customers.Count, Converter={StaticResource ZeroToVisibilityConverter}}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="暂无客户" FontSize="20" Foreground="{StaticResource QuaternaryLabelBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="点击「添加客户」开始创建" FontSize="13" Foreground="{StaticResource QuaternaryLabelBrush}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
            </Grid>
        </Border>

        <!-- 分页栏 -->
        <Border Grid.Row="3" Background="{StaticResource SurfaceBrush}" Padding="24,12"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock VerticalAlignment="Center">
                    <Run Text="共"/>
                    <Run Text="{Binding TotalCount}"/>
                    <Run Text="条记录"/>
                </TextBlock>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="首页" Command="{Binding FirstPageCommand}"
                            Style="{StaticResource PaginationButtonStyle}" Margin="2,0"/>
                    <Button Content="上一页" Command="{Binding PreviousPageCommand}"
                            Style="{StaticResource PaginationButtonStyle}" Margin="2,0"/>
                    <TextBlock VerticalAlignment="Center" Margin="12,0">
                        <Run Text="第"/>
                        <Run Text="{Binding PageIndex}"/>
                        <Run Text="/"/>
                        <Run Text="{Binding TotalPages}"/>
                        <Run Text="页"/>
                    </TextBlock>
                    <Button Content="下一页" Command="{Binding NextPageCommand}"
                            Style="{StaticResource PaginationButtonStyle}" Margin="2,0"/>
                    <Button Content="末页" Command="{Binding LastPageCommand}"
                            Style="{StaticResource PaginationButtonStyle}" Margin="2,0"/>
                    
                    <ComboBox SelectedValue="{Binding PageSize}" SelectedValuePath="Tag" Margin="16,0,0,0" Width="80">
                        <ComboBoxItem Content="50" Tag="50"/>
                        <ComboBoxItem Content="100" Tag="100" IsSelected="True"/>
                        <ComboBoxItem Content="200" Tag="200"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 加载中 -->
        <Border Grid.RowSpan="4" Background="#80FFFFFF" 
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200"/>
                <TextBlock Text="加载中..." Margin="0,8,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
